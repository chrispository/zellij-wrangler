[package]
name = "zjw"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "pane-comms companion CLI: cross-pane/cross-tab send, ask, wait, listen on stock zellij"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
