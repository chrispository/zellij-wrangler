[package]
name = "hub"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "pane-comms hub plugin: named channels, cross-pane read/write, wait-for-output for zellij"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zellij-tile = "=0.44.3"
